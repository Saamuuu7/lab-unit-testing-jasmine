function calculateArea(num1, num2) {
    if (typeof num1 != "number" || typeof num2 != "number") {
        return undefined
    }
    // formula para calcular el area de un rectángulo
    return num1 * num2
}